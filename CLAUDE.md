# AdamenBlog — инструкции для Claude

## Архитектура проекта

Статический блог-портфолио на Vite + React + TypeScript. Контент — `.md` файлы, бэкенда нет.

- `src/content/posts/*.md` — посты (каждый семестр = отдельный пост)
- `src/content/structure.json` — навигация (курсы → семестры)
- `public/works/` — локальные файлы работ (gitignored, 600MB)
- `src/components/FileLink.tsx` — умный компонент для ссылок с fallback

## Ссылки в постах: формат

Каждая ссылка в `.md` файлах хранит **оба пути** в одной строке:

```markdown
[Метка](https://drive.google.com/file/d/FILE_ID/view "/works/N%20курс/N%20семестр/папка/файл.ext")
```

- `href` — Google Drive (основной, работает везде онлайн)
- `title` — локальный путь (fallback оффлайн)

### Логика FileLink компонента

- **Онлайн** (любая среда) → открывает Drive ссылку
- **Оффлайн + localhost** → открывает локальный файл
- Реагирует на `window online/offline` события в реальном времени

### URL-кодирование в локальных путях

Обязательно кодировать:
- пробелы → `%20`
- `(` → `%28`
- `)` → `%29`

Оставлять как есть (браузеры и Vite обрабатывают):
- кириллица
- `№`, `,`, `.`

## Как заполнять семестр

### Шаг 1: Изучить структуру локальных папок

```bash
find "public/works/N курс/N семестр" -maxdepth 1 -type d | sort
```

Папки внутри = дисциплины. Некоторые дисциплины могут иметь подпапки (лр 1, лр 2, курсовая и т.д.).

```bash
find "public/works/N курс/N семестр" -maxdepth 2 -type f | grep -v "~\$" | sort
```

### Шаг 2: Получить Drive ID папки семестра

Корень Drive: `https://drive.google.com/drive/u/0/folders/1HPhKUHxGW8X0ddNwwbY1c3URK9132Oq6`

Навигация через embed API (возвращает ID без JS):
```
https://drive.google.com/embeddedfolderview?id=FOLDER_ID
```

Иерархия ID:
| Папка | Drive ID |
|---|---|
| Корень | `1HPhKUHxGW8X0ddNwwbY1c3URK9132Oq6` |
| 1 курс | `13Jt9-bH7VBpVdYnsquFS4TA8skyR1IKm` |
| 1 курс / 1 семестр | `13m5TLPzsjpGd2UX_m4DuTIvl-nky_Kj1` |
| 1 курс / 2 семестр | `13m-QkB-rxHvEbXnQMqts_SDbT588cUOT` |
| 2 курс | `1-8KcZJCUfWYOQTmdV-StcA2fZP1obEaX` |
| 3 курс | `1QRfOYSlg8mS0QeSy3U4bhQyhR071vBNQ` |
| 4 курс | `1sx8RSS7RIJFusN9-BBy8UWsXbFZ3iP7z` |

Для новых семестров — сначала получить ID через WebFetch на embeddedfolderview родительской папки.

### Шаг 3: Получить ID файлов в Drive

Для каждой папки дисциплины:
```
WebFetch: https://drive.google.com/embeddedfolderview?id=DISCIPLINE_FOLDER_ID
Prompt: "Extract ALL file names and their Google Drive IDs"
```

Запускать параллельно для всех дисциплин семестра.

### Шаг 4: Составить `.md` файл

Структура поста:

```markdown
---
id: N
title: "N Семестр"
---

## Название дисциплины

- [ЛР №1](https://drive.google.com/file/d/FILE_ID/view "/works/N%20курс/N%20семестр/папка/файл.docx")
- [ЛР №2](https://drive.google.com/file/d/FILE_ID/view "/works/N%20курс/N%20семестр/папка/файл.pdf")

## Другая дисциплина

```

Правила именования меток:
- Лабораторные: `ЛР №1`, `ЛР №2`, ...
- Практические задания: `ПЗ Тема №1`
- Самостоятельные работы: `СР Тема №1`
- Внеаудиторные СР: `ВСР 1.1.2`
- Индивидуальные СР: `ИСР Задание 1`
- Курсовые: `Курсовая работа`
- Презентации: `Презентация`

Если в Drive папка дисциплины содержит **подпапки** (лр 1, лр 2, ...):
- Drive ссылка → на папку: `https://drive.google.com/drive/folders/FOLDER_ID`
- Локальная ссылка → на конкретный файл внутри подпапки

### Шаг 5: Проверить все ссылки

**Локальные пути** (пока запущен dev server):
```bash
BASE="http://localhost:5173/works/N%20курс/N%20семестр"
curl -s -o /dev/null -w "метка: %{http_code}\n" "$BASE/папка/файл.ext" &
# ... все ссылки параллельно ...
wait
```
Все должны вернуть `200`. Если `404` — ошибка в пути или кодировке.

**Drive ссылки:**
```bash
curl -s -o /dev/null -w "метка: %{http_code}\n" -L --max-time 5 "https://drive.google.com/file/d/ID/view" &
# ... все ссылки параллельно ...
wait
```
Все должны вернуть `200`.

## Структура posts/*.md по семестрам

| Post ID | Файл | Семестр | Курс |
|---|---|---|---|
| 2 | `2.md` | 1 Семестр | Курс 1 |
| 3 | `3.md` | 2 Семестр | Курс 1 |
| 4 | `4.md` | 3 Семестр | Курс 2 |
| 5 | `5.md` | 4 Семестр | Курс 2 |
| 6 | `6.md` | 5 Семестр | Курс 3 |
| 7 | `7.md` | 6 Семестр | Курс 3 |
| 13 | `13.md` | 7 Семестр | Курс 4 |
| 14 | `14.md` | 8 Семестр | Курс 4 |

## Деплой

- **Vercel**: `public/works/` не включается (gitignored) → Drive ссылки работают автоматически
- **Локально с интернетом**: Drive ссылки
- **Локально без интернета**: автоматический fallback на `public/works/`

## Частые ошибки

1. **404 на локальный файл** — проверить кодировку пути, особенно `(` → `%28`, `)` → `%29`, пробелы → `%20`
2. **Подпапки в дисциплине** — Drive и локальная структура должны совпадать; ссылка в title должна вести на конкретный файл, не на папку
3. **Drive ссылка не открывается** — убедиться что файл публично доступен в Drive (папка должна быть "Доступно всем по ссылке")
4. **Fallback срабатывает онлайн** — проверить что `FileLink.tsx` использует условие `!online && isLocalhost`, а не только `!online`
